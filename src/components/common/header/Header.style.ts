import { createStyles } from '@material-ui/core/styles';

const drawerWidth = 1040;

const styles = createStyles({
    drawer: {
        marginLeft: drawerWidth,
    },
    logoLink: {
        color: 'inherit',
        textDecoration: 'none',
    },
});

export default styles;
